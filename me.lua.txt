local Rayfield = loadstring(game:HttpGet('https://raw.githubusercontent.com/shlexware/Rayfield/main/source'))()

-- Window Konfiguration mit Custom Theme
local Window = Rayfield:CreateWindow({
    Name = "Premium Script v1.0",
    LoadingTitle = "Loading Interface...",
    LoadingSubtitle = "by YourName",
    ConfigurationSaving = {
        Enabled = true,
        FolderName = "ScriptSettings",
        FileName = "ConfigFile"
    },
    KeySystem = true,
    KeySettings = {
        Title = "Key System",
        Subtitle = "Enter Key to Access",
        Note = "Join Discord for key",
        Key = "PREMIUM-KEY-123"
    },
    Discord = {
        Enabled = true,
        Invite = "discord.gg/YourServer",
        RememberJoins = true
    }
})

-- Custom Theme
Rayfield:LoadConfiguration({
    Theme = {
        Main = Color3.fromRGB(35, 35, 35),
        Second = Color3.fromRGB(25, 25, 25),
        Stroke = Color3.fromRGB(50, 50, 50),
        Accent = Color3.fromRGB(0, 170, 255),
        TextColor = Color3.fromRGB(255, 255, 255)
    }
})

-- Tabs erstellen
local MainTab = Window:CreateTab("Main", 4483362458)
local SettingsTab = Window:CreateTab("Settings", 4483362458)
local CreditsTab = Window:CreateTab("Credits", 4483362458)

-- Main Tab Elemente
local ToggleButton = MainTab:CreateToggle({
    Name = "Auto Farm",
    CurrentValue = false,
    Flag = "AutoFarmToggle",
    Callback = function(Value)
        -- Auto Farm Logic
    end,
})

local Slider = MainTab:CreateSlider({
    Name = "Walk Speed",
    Range = {16, 500},
    Increment = 1,
    Suffix = "Speed",
    CurrentValue = 16,
    Flag = "SpeedSlider",
    Callback = function(Value)
        game.Players.LocalPlayer.Character.Humanoid.WalkSpeed = Value
    end,
})

local Dropdown = MainTab:CreateDropdown({
    Name = "Teleport Location",
    Options = {"Spawn", "Shop", "Farm Area", "Boss Room"},
    CurrentOption = "Spawn",
    Flag = "TeleportDropdown",
    Callback = function(Option)
        -- Teleport Logic
    end,
})

-- Keybind System
local Keybind = MainTab:CreateKeybind({
    Name = "Toggle GUI",
    CurrentKeybind = "RightControl",
    HoldToInteract = false,
    Flag = "ToggleGuiKeybind",
    Callback = function()
        -- Toggle GUI Logic
    end,
})

-- Settings Tab
local ColorPicker = SettingsTab:CreateColorPicker({
    Name = "UI Color",
    Color = Color3.fromRGB(0, 170, 255),
    Flag = "UIColor",
    Callback = function(Color)
        -- Change UI Color Logic
    end
})

-- Paragraph mit wichtigen Informationen
CreditsTab:CreateParagraph({
    Title = "Script Information",
    Content = "Created by: YourName\nVersion: 1.0\nLast Updated: "..os.date("%d/%m/%Y")
})

-- Button mit Notification
local Button = CreditsTab:CreateButton({
    Name = "Copy Discord",
    Callback = function()
        setclipboard("discord.gg/YourServer")
        Rayfield:Notify({
            Title = "Discord Copied",
            Content = "Discord invite has been copied to clipboard!",
            Duration = 3,
            Image = 4483362458
        })
    end,
})

-- Section für gruppierte Elemente
local Section = MainTab:CreateSection("Advanced Settings")

-- Input field
local Input = MainTab:CreateInput({
    Name = "Custom Value",
    PlaceholderText = "Enter value here",
    RemoveTextAfterFocusLost = false,
    Callback = function(Text)
        -- Input Logic
    end,
})
