local Rayfield = loadstring(game:HttpGet('https://raw.githubusercontent.com/shlexware/Rayfield/main/source'))()

-- Window Konfiguration
local Window = Rayfield:CreateWindow({
    Name = "Premium Script Hub",
    LoadingTitle = "Loading Interface...",
    LoadingSubtitle = "by YourName",
    ConfigurationSaving = {
        Enabled = true,
        FolderName = "ScriptSettings",
        FileName = "ConfigFile"
    },
    KeySystem = true,
    KeySettings = {
        Title = "Key System",
        Subtitle = "Enter Key to Access",
        Note = "Join Discord for key",
        Key = "PREMIUM-KEY-123"
    },
    Discord = {
        Enabled = true,
        Invite = "discord.gg/YourServer",
        RememberJoins = true
    },
    Theme = {
        Main = Color3.fromRGB(35, 35, 35),
        Second = Color3.fromRGB(25, 25, 25),
        Stroke = Color3.fromRGB(50, 50, 50),
        Accent = Color3.fromRGB(0, 170, 255),
        TextColor = Color3.fromRGB(255, 255, 255)
    }
})

-- Tabs erstellen
local MainTab = Window:CreateTab("Main", 4483362458)
local SettingsTab = Window:CreateTab("Settings", 4483362458)
local CreditsTab = Window:CreateTab("Credits", 4483362458)
local AnimationsTab = Window:CreateTab("Animations", 4483362458)

-- Main Tab Elemente
local ToggleButton = MainTab:CreateToggle({
    Name = "Auto Farm",
    CurrentValue = false,
    Flag = "AutoFarmToggle",
    Callback = function(Value)
        -- Auto Farm Logic
    end,
})

local Slider = MainTab:CreateSlider({
    Name = "Walk Speed",
    Range = {16, 500},
    Increment = 1,
    Suffix = "Speed",
    CurrentValue = 16,
    Flag = "SpeedSlider",
    Callback = function(Value)
        game.Players.LocalPlayer.Character.Humanoid.WalkSpeed = Value
    end,
})

local Dropdown = MainTab:CreateButton({
    Name = "Teleport to Spawn",
    Callback = function()
        -- Teleport Logic
    end,
})

-- Settings Tab
local ColorPicker = SettingsTab:CreateColorPicker({
    Name = "UI Color",
    Color = Color3.fromRGB(0, 170, 255),
    Flag = "UIColor",
    Callback = function(Color)
        -- Change UI Color Logic
    end
})

local Dropdown = SettingsTab:CreateDropdown({
    Name = "Notification Sound",
    Options = {"Sound 1", "Sound 2", "Sound 3"},
    CurrentOption = "Sound 1",
    Flag = "NotificationSound",
    Callback = function(Option)
        -- Change Notification Sound Logic
    end,
})

-- Credits Tab
local CreditsButton = CreditsTab:CreateButton({
    Name = "Copy Discord",
    Callback = function()
        setclipboard("discord.gg/YourServer")
        Rayfield:Notify({
            Title = "Discord Copied",
            Content = "Discord invite has been copied to clipboard!",
            Duration = 3,
            Image = 4483362458
        })
    end,
})

-- Animations Tab
local AnimationButton = AnimationsTab:CreateButton({
    Name = "Play Animation",
    Callback = function()
        -- Play Animation Logic
    end,
})

local AnimationSlider = AnimationsTab:CreateSlider({
    Name = "Animation Speed",
    Range = {0.1, 10},
    Increment = 0.1,
    Suffix = "Speed",
    CurrentValue = 1,
    Flag = "AnimationSpeed",
    Callback = function(Value)
        -- Change Animation Speed Logic
    end,
})

-- Section für gruppierte Elemente
local Section = MainTab:CreateSection("Advanced Settings")

-- Input field
local Input = MainTab:CreateInput({
    Name = "Custom Value",
    PlaceholderText = "Enter value here",
    RemoveTextAfterFocusLost = false,
    Callback = function(Text)
        -- Input Logic
    end,
})

-- Notify Funktion
Rayfield:Notify({
    Title = "Script Loaded",
    Content = "The script has been loaded successfully!",
    Duration = 3,
    Image = 4483362458
})
